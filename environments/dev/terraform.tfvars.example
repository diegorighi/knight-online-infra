# Knight Online Server - Development Environment Variables
# Copy this file to terraform.tfvars and fill in the values

# AWS Region
aws_region = "us-east-1"

# Project name (used for resource naming)
project_name = "knight-online-dev"

# IMPORTANT: Replace with your public IP for secure access
# Get your IP: curl ifconfig.me
admin_ip_cidrs = ["YOUR.IP.ADDRESS.HERE/32"]

# Key pair configuration
# Option 1: Use existing key pair
existing_key_name = "your-existing-key-name"

# Option 2: Create new key pair
# create_key_pair = true
# public_key = "ssh-rsa AAAAB3..."

# Game Server configuration
game_server_instance_type = "t3.medium"  # 2 vCPU, 4GB RAM (~$30/month)
game_server_volume_size   = 50           # GB

# Web Server (optional)
create_web_server         = false
web_server_instance_type  = "t3.small"   # 2 vCPU, 2GB RAM (~$15/month)
web_server_volume_size    = 20           # GB
